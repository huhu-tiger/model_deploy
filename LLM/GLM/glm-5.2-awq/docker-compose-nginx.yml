services:
  nginx-llm-proxy:
    # 使用 OpenResty 以便用 Lua 在 rewrite 阶段读请求体并写入日志变量（标准 nginx 的 $request_body 在 proxy 场景不生效）
    image: model.vnet.com/sjhl/openresty:1.27.1.2-0-alpine
    container_name: nginx-glm-5.2-awq-proxy
    restart: unless-stopped
    ports:
      - "30002:80"
    user: "0:0"
    environment:
      TZ: Asia/Shanghai
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./conf.d:/etc/nginx/conf.d:ro
      - ./logs:/var/log/nginx
      - /usr/share/zoneinfo/Asia/Shanghai:/etc/localtime:ro
    command: ["openresty", "-c", "/etc/nginx/nginx.conf", "-g", "daemon off;"]
    depends_on:
      - glm-5.2-awq-int4-vllm

  glm-5.2-awq-int4-vllm:
    image: ${IMAGE:-model.vnet.com/sjhl/vllm-openai:glm52}
    container_name: GLM-5.2-AWQ-INT4-vLLM
    restart: always
    runtime: nvidia
    command: >
      /media/llm/cyankiwi/GLM-5.2-AWQ-INT4
      --served-model-name WanWu/GLM-Auto
      --host 0.0.0.0
      --port 30003
      --tensor-parallel-size 8
      --trust-remote-code
      --gpu-memory-utilization 0.90
      --max-model-len 131072
      --kv-cache-dtype fp8
      --max-num-seqs 64
      --enable-chunked-prefill
      --enable-prefix-caching
      --tool-call-parser glm47
      --reasoning-parser glm45
      --enable-auto-tool-choice
      --override-generation-config '{"speculative_config": null}'
    environment:
      TZ: Asia/Shanghai
      NVIDIA_VISIBLE_DEVICES: "0,1,2,3,4,5,6,7"
      VLLM_USE_MODELSCOPE: "false"
      NCCL_DEBUG: "warn"
      # DeepGEMM JIT 预热跳过（GLM-5.2 系列使用 DeepGEMM 内核，首次请求时按需编译）
      VLLM_DEEP_GEMM_WARMUP: "skip"
    volumes:
      - /media/llm:/media/llm
      - /usr/share/zoneinfo/Asia/Shanghai:/etc/localtime:ro
    ulimits:
      memlock: -1
      stack: 67108864
    ipc: host
    shm_size: "32g"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://127.0.0.1:30003/health"]
      interval: 300s
      timeout: 10s
      retries: 3
      start_period: 1200s
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              device_ids: ['0', '1', '2', '3', '4', '5', '6', '7']
              capabilities: [gpu]
